import { CheckBox, ComboBox, LineEdit, ScrollView, SpinBox, TextEdit } from "std-widgets.slint";

component ActionButton inherits Rectangle {
    in property <string> label;
    in property <bool> primary: false;
    in property <bool> enabled: true;
    in property <color> accent;
    in property <color> fill;
    in property <color> text-color;
    in property <color> line-color;
    callback clicked();

    height: 32px;
    border-radius: 7px;
    background: !root.enabled ? root.line-color : (root.primary ? root.accent : root.fill);
    border-width: 1px;
    border-color: root.primary ? root.accent : root.line-color;

    Text {
        text: root.label;
        color: !root.enabled ? #94a3b8 : (root.primary ? #ffffff : root.text-color);
        font-size: 12px;
        font-weight: 600;
        horizontal-alignment: center;
        vertical-alignment: center;
    }

    TouchArea {
        enabled: root.enabled;
        clicked => { root.clicked(); }
    }
}

component SidebarEntry inherits Rectangle {
    in property <string> icon;
    in property <string> title;
    in property <string> subtitle;
    in property <string> badge: "";
    in property <bool> active: false;
    in property <color> accent;
    in property <color> base-bg;
    in property <color> text-color;
    in property <color> muted-color;
    in property <color> outline-color;

    height: 44px;
    border-radius: 8px;
    background: root.active ? root.base-bg : #00000000;
    border-width: root.active ? 1px : 0px;
    border-color: root.active ? root.outline-color : #00000000;

    HorizontalLayout {
        padding-left: 10px;
        padding-right: 10px;
        padding-top: 6px;
        padding-bottom: 6px;
        spacing: 9px;

        Rectangle {
            width: 28px;
            height: 28px;
            border-radius: 7px;
            background: root.active ? root.accent : root.base-bg;

            Text {
                text: root.icon;
                color: root.active ? #ffffff : root.text-color;
                font-size: 12px;
                font-weight: 600;
                horizontal-alignment: center;
                vertical-alignment: center;
            }
        }

        VerticalLayout {
            spacing: 1px;
            Text {
                text: root.title;
                color: root.text-color;
                font-size: 13px;
                font-weight: 600;
            }
            Text {
                text: root.subtitle;
                color: root.muted-color;
                font-size: 11px;
            }
        }

        Rectangle { }

        if root.badge != "" : Rectangle {
            width: 48px;
            height: 20px;
            border-radius: 8px;
            background: root.accent;

            Text {
                text: root.badge;
                color: #ffffff;
                font-size: 10px;
                font-weight: 600;
                horizontal-alignment: center;
                vertical-alignment: center;
            }
        }
    }
}

component StatusCard inherits Rectangle {
    in property <string> label;
    in property <string> value;
    in property <string> icon;
    in property <color> accent;
    in property <color> base-bg;
    in property <color> outline-color;
    in property <color> text-color;
    in property <color> muted-color;

    border-radius: 8px;
    border-width: 1px;
    border-color: root.outline-color;
    background: root.base-bg;

    VerticalLayout {
        padding: 12px;
        spacing: 8px;

        HorizontalLayout {
            Text {
                text: root.label;
                color: root.muted-color;
                font-size: 12px;
                font-weight: 600;
            }
            Rectangle { }
            Rectangle {
                width: 24px;
                height: 24px;
                border-radius: 6px;
                background: root.accent;

                Text {
                    text: root.icon;
                    color: #ffffff;
                    font-size: 11px;
                    font-weight: 600;
                    horizontal-alignment: center;
                    vertical-alignment: center;
                }
            }
        }

        Text {
            text: root.value;
            color: root.text-color;
            font-size: 17px;
            font-weight: 600;
        }
    }
}

component FeatureCard inherits Rectangle {
    in property <string> title;
    in property <string> desc;
    in property <string> meta;
    in property <bool> enabled: true;
    in property <color> accent;
    in property <color> base-bg;
    in property <color> outline-color;
    in property <color> text-color;
    in property <color> muted-color;

    border-radius: 8px;
    border-width: 1px;
    border-color: root.outline-color;
    background: root.base-bg;

    HorizontalLayout {
        padding: 12px;
        spacing: 12px;

        Rectangle {
            width: 38px;
            height: 22px;
            border-radius: 8px;
            background: root.enabled ? root.accent : root.border-color;

            Rectangle {
                y: 3px;
                x: root.enabled ? 19px : 3px;
                width: 16px;
                height: 16px;
                border-radius: 6px;
                background: #ffffff;
            }
        }

        VerticalLayout {
            spacing: 4px;
            HorizontalLayout {
                spacing: 8px;
                Text {
                    text: root.title;
                    color: root.text-color;
                    font-size: 13px;
                    font-weight: 600;
                }
                if root.enabled : Rectangle {
                    width: 34px;
                    height: 18px;
                    border-radius: 8px;
                    background: root.accent;

                    Text {
                        text: "启用";
                        color: #ffffff;
                        font-size: 9px;
                        font-weight: 600;
                        horizontal-alignment: center;
                        vertical-alignment: center;
                    }
                }
            }
            Text {
                text: root.desc;
                color: root.muted-color;
                font-size: 11px;
            }
            Text {
                text: root.meta;
                color: root.accent;
                font-size: 11px;
                font-weight: 600;
            }
        }
    }
}

component PlanCard inherits Rectangle {
    in property <string> title;
    in property <string> desc;
    in property <string> tags;
    in property <color> accent;
    in property <color> base-bg;
    in property <color> outline-color;
    in property <color> text-color;
    in property <color> muted-color;

    border-radius: 8px;
    border-width: 1px;
    border-color: root.outline-color;
    background: root.base-bg;

    VerticalLayout {
        padding: 12px;
        spacing: 8px;

        HorizontalLayout {
            Text {
                text: root.title;
                color: root.text-color;
                font-size: 13px;
                font-weight: 600;
            }
            Rectangle { }
            Rectangle {
                width: 44px;
                height: 18px;
                border-radius: 8px;
                background: root.accent;

                Text {
                    text: "规划中";
                    color: #ffffff;
                    font-size: 9px;
                    font-weight: 600;
                    horizontal-alignment: center;
                    vertical-alignment: center;
                }
            }
        }

        Text {
            text: root.desc;
            color: root.muted-color;
            font-size: 11px;
            wrap: word-wrap;
        }

        Rectangle {
            height: 28px;
            border-radius: 8px;
            background: root.border-color;

            Text {
                text: root.tags;
                color: root.muted-color;
                font-size: 10px;
                horizontal-alignment: center;
                vertical-alignment: center;
            }
        }
    }
}

component ThemeSwatch inherits Rectangle {
    in property <color> swatch;
    in property <string> label;
    in property <bool> selected: false;
    in property <color> fill;
    in property <color> text-color;
    in property <color> line-color;
    callback activate();

    height: 64px;
    border-radius: 8px;
    border-width: 1px;
    border-color: root.selected ? root.swatch : root.line-color;
    background: root.fill;

    VerticalLayout {
        padding: 8px;
        spacing: 6px;

        Rectangle {
            height: 34px;
            border-radius: 7px;
            background: root.swatch;

            if root.selected : Text {
                text: "OK";
                color: #ffffff;
                font-size: 10px;
                font-weight: 600;
                horizontal-alignment: center;
                vertical-alignment: center;
            }
        }

        Text {
            text: root.label;
            color: root.text-color;
            font-size: 10px;
            horizontal-alignment: center;
        }
    }

    TouchArea {
        clicked => { root.activate(); }
    }
}

export component MainWindow inherits Window {
    title: "Bilibili Danmu Robot";
    width: 1440px;
    height: 920px;

    in-out property <string> room-id: "8792912";
    in-out property <int> danmu-len: 20;
    in-out property <string> entry-msg: "花花机器人进入直播间";
    in-out property <string> goodbye-info: "下播啦~ 感谢大家的陪伴~ 下次见哦~";
    in-out property <bool> pk-notice: true;
    in-out property <bool> welcome-enabled: true;
    in-out property <bool> entry-effect: true;
    in-out property <bool> thanks-gift: true;
    in-out property <bool> thanks-focus: true;
    in-out property <bool> thanks-share: true;
    in-out property <bool> cron-danmu: false;
    in-out property <bool> keyword-reply: false;
    in-out property <bool> draw-by-lot: true;
    in-out property <bool> sign-in-enable: true;
    in-out property <bool> danmu-filter-enable: false;
    in-out property <string> danmu-filter-words-text: "";
    in-out property <int> danmu-filter-repeat-threshold: 3;
    in-out property <string> gift-aliases-text: "";
    in-out property <string> gift-thanks-templates-text: "";
    in-out property <bool> gift-summary-thanks: false;
    in-out property <string> gift-summary-template: "本轮共收到{count}件礼物，价值{value}电池";
    in-out property <bool> newcomer-danmu-enable: false;
    in-out property <string> newcomer-danmu-template: "欢迎新朋友 {user} 首次发言";
    in-out property <string> permanent-blacklist-users-text: "";
    in-out property <string> permanent-blacklist-names-text: "";
    in-out property <string> special-nicknames-text: "";
    in-out property <string> robot-name: "花花";
    in-out property <string> talk-robot-cmd: "花花";
    in-out property <int> robot-mode-index: 0;
    in-out property <string> chatgpt-token: "";
    in-out property <string> chatgpt-api-url: "";
    in-out property <string> chatgpt-prompt: "你是一个非常幽默的机器人助理，可以使用emoji表情，可以使用颜文字";
    in-out property <string> welcome-list-text: "";
    in-out property <string> focus-list-text: "";
    in-out property <string> blacklist-wide-text: "";
    in-out property <string> blacklist-exact-text: "";
    in-out property <string> keyword-reply-text: "";
    in-out property <string> cron-danmu-text: "";
    in-out property <string> draw-list-text: "";
    in-out property <string> manual-danmu: "";
    in-out property <string> login-url: "";
    in-out property <string> user-info: "夜莺工作室";
    in-out property <string> room-status: "8792912 / 直播中";
    in-out property <string> run-status: "运行中";
    in-out property <string> update-link: "";
    in-out property <string> log-text: "";
    in-out property <string> session-danmu-count: "0";
    in-out property <string> session-entry-count: "0";
    in-out property <string> session-follow-count: "0";
    in-out property <string> session-gift-value: "0";
    in-out property <string> session-guard-buyer-count: "0";
    in-out property <string> session-popularity: "0 / 0";
    in-out property <string> session-pk-summary: "暂无 PK";
    in-out property <string> user-query-uid: "";
    in-out property <string> user-detail-text: "输入 UID 查询观众详情";

    in-out property <bool> theme-popup-open: false;
    in-out property <bool> dark-mode: false;
    in-out property <int> theme-choice: 0;

    property <color> theme-primary:
        root.theme-choice == 1 ? #34c759 :
        root.theme-choice == 2 ? #ff9500 :
        root.theme-choice == 3 ? #ff2d55 :
        root.theme-choice == 4 ? #8e8e93 :
        root.theme-choice == 5 ? #ffcc00 :
        root.theme-choice == 6 ? #af52de :
        root.theme-choice == 7 ? #ff3b30 :
        root.theme-choice == 8 ? #5ac8fa :
        root.theme-choice == 9 ? #af52de :
        root.theme-choice == 10 ? #30d158 :
        root.theme-choice == 11 ? #5856d6 :
        #007aff;

    property <color> app-bg: root.dark-mode ? #1c1c1e : #f5f5f7;
    property <color> panel-bg: root.dark-mode ? #2c2c2e : #ffffff;
    property <color> panel-soft-bg: root.dark-mode ? #3a3a3c : #f2f2f7;
    property <color> panel-strong-bg: root.dark-mode ? #111113 : #e5e5ea;
    property <color> line-color: root.dark-mode ? #48484a : #d1d1d6;
    property <color> text-color: root.dark-mode ? #f5f5f7 : #1d1d1f;
    property <color> muted-color: root.dark-mode ? #a1a1a6 : #6e6e73;
    property <color> card-alt-bg: root.dark-mode ? #3a3a3c : #f9f9fb;
    property <color> success-bg: root.dark-mode ? #10351f : #e8f7ee;
    property <color> success-text: root.dark-mode ? #30d158 : #248a3d;
    property <color> warning-bg: root.dark-mode ? #3a2a08 : #fff4d8;
    property <color> warning-text: root.dark-mode ? #ffd60a : #946800;
    property <color> popup-bg: root.dark-mode ? #2c2c2e : #ffffff;

    callback load-config();
    callback save-config(
        string, int, string, string,
        bool, bool, bool, bool, bool, bool, bool, bool, bool, bool, bool,
        string, int, string, string, bool, string, bool, string, string, string, string, string, string, int,
        string, string, string, string, string, string, string, string, string, string
    );
    callback start-login();
    callback check-login();
    callback check-room();
    callback start-monitor();
    callback stop-monitor();
    callback send-danmu(string);
    callback query-user-detail(string);
    callback check-update();
    callback download-update(string);

    background: root.app-bg;

    Rectangle {
        background: root.app-bg;

        HorizontalLayout {
            spacing: 0px;

            Rectangle {
                width: 244px;
                background: root.panel-soft-bg;
                border-width: 1px;
                border-color: root.line-color;

                VerticalLayout {
                    padding-left: 16px;
                    padding-right: 16px;
                    padding-top: 18px;
                    padding-bottom: 18px;
                    spacing: 12px;

                    HorizontalLayout {
                        spacing: 10px;
                        Rectangle {
                            width: 40px;
                            height: 40px;
                            border-radius: 8px;
                            background: root.theme-primary;

                            Text {
                                text: "LB";
                                color: #ffffff;
                                font-size: 13px;
                                font-weight: 600;
                                horizontal-alignment: center;
                                vertical-alignment: center;
                            }
                        }
                        VerticalLayout {
                            spacing: 2px;
                            Text { text: "Live Bot"; color: root.text-color; font-size: 18px; font-weight: 600; }
                            Text { text: "直播运营助手"; color: root.muted-color; font-size: 11px; font-weight: 600; }
                        }
                    }

                    Rectangle { height: 1px; background: root.line-color; }

                    Text { text: "工作区"; color: root.muted-color; font-size: 11px; font-weight: 600; }
                    SidebarEntry { icon: "总"; title: "总览"; subtitle: "运营中心"; active: true; accent: root.theme-primary; base-bg: root.panel-soft-bg; text-color: root.text-color; muted-color: root.muted-color; outline-color: root.line-color; }
                    SidebarEntry { icon: "登"; title: "登录与房间"; subtitle: "账号管理"; accent: root.theme-primary; base-bg: root.panel-soft-bg; text-color: root.text-color; muted-color: root.muted-color; outline-color: root.line-color; }
                    SidebarEntry { icon: "听"; title: "监听与发送"; subtitle: "弹幕控制"; accent: root.theme-primary; base-bg: root.panel-soft-bg; text-color: root.text-color; muted-color: root.muted-color; outline-color: root.line-color; }
                    SidebarEntry { icon: "规"; title: "自动回复规则"; subtitle: "互动策略"; accent: root.theme-primary; base-bg: root.panel-soft-bg; text-color: root.text-color; muted-color: root.muted-color; outline-color: root.line-color; }
                    SidebarEntry { icon: "AI"; title: "AI 机器人"; subtitle: "智能对话"; accent: root.theme-primary; base-bg: root.panel-soft-bg; text-color: root.text-color; muted-color: root.muted-color; outline-color: root.line-color; }
                    SidebarEntry { icon: "数"; title: "数据统计"; subtitle: "运营分析"; accent: root.theme-primary; base-bg: root.panel-soft-bg; text-color: root.text-color; muted-color: root.muted-color; outline-color: root.line-color; }
                    SidebarEntry { icon: "PK"; title: "PK 与活动"; subtitle: "赛事管理"; accent: root.theme-primary; base-bg: root.panel-soft-bg; text-color: root.text-color; muted-color: root.muted-color; outline-color: root.line-color; }
                    SidebarEntry { icon: "房"; title: "直播间管理"; subtitle: "房间配置"; badge: "规划中"; accent: root.theme-primary; base-bg: root.panel-soft-bg; text-color: root.text-color; muted-color: root.muted-color; outline-color: root.line-color; }
                    SidebarEntry { icon: "扩"; title: "高级扩展"; subtitle: "插件生态"; badge: "规划中"; accent: root.theme-primary; base-bg: root.panel-soft-bg; text-color: root.text-color; muted-color: root.muted-color; outline-color: root.line-color; }
                    SidebarEntry { icon: "录"; title: "录播与素材"; subtitle: "内容存档"; badge: "规划中"; accent: root.theme-primary; base-bg: root.panel-soft-bg; text-color: root.text-color; muted-color: root.muted-color; outline-color: root.line-color; }
                    SidebarEntry { icon: "系"; title: "系统与更新"; subtitle: "版本管理"; accent: root.theme-primary; base-bg: root.panel-soft-bg; text-color: root.text-color; muted-color: root.muted-color; outline-color: root.line-color; }

                    Rectangle { height: 1px; background: root.line-color; }

                    Rectangle {
                        height: 74px;
                        border-radius: 8px;
                        background: root.panel-bg;
                        border-width: 1px;
                        border-color: root.line-color;

                        VerticalLayout {
                            padding: 12px;
                            spacing: 4px;
                            Text {
                                text: root.run-status == "运行中" ? "监听正在运行" : "监听未启动";
                                color: root.text-color;
                                font-size: 13px;
                                font-weight: 600;
                            }
                            Text {
                                text: "房间 " + root.room-id + " · " + root.room-status;
                                color: root.muted-color;
                                font-size: 11px;
                            }
                            Text {
                                text: "主题弹窗默认展开，可即时切换亮暗与配色";
                                color: root.muted-color;
                                font-size: 10px;
                            }
                        }
                    }
                }
            }

            VerticalLayout {
                spacing: 0px;

                Rectangle {
                    height: 64px;
                    background: root.panel-bg;
                    border-width: 1px;
                    border-color: root.line-color;

                    HorizontalLayout {
                        padding-left: 22px;
                        padding-right: 22px;
                        padding-top: 14px;
                        padding-bottom: 14px;
                        spacing: 16px;

                        VerticalLayout {
                            spacing: 2px;
                            Text { text: "直播间 #" + root.room-id; color: root.text-color; font-size: 16px; font-weight: 600; }
                            Text { text: "主播账号: " + root.user-info; color: root.muted-color; font-size: 12px; }
                        }

                        Rectangle { width: 1px; background: root.line-color; }

                        Rectangle {
                            width: 96px;
                            height: 34px;
                            border-radius: 8px;
                            background: root.run-status == "运行中" ? root.success-bg : root.warning-bg;
                            border-width: 1px;
                            border-color: root.run-status == "运行中" ? root.success-text : root.warning-text;

                            Text {
                                text: root.run-status;
                                color: root.run-status == "运行中" ? root.success-text : root.warning-text;
                                font-size: 12px;
                                font-weight: 600;
                                horizontal-alignment: center;
                                vertical-alignment: center;
                            }
                        }

                        Rectangle { }

                        Rectangle {
                            width: 280px;
                            height: 40px;
                            border-radius: 8px;
                            background: root.card-alt-bg;
                            border-width: 1px;
                            border-color: root.line-color;

                            LineEdit {
                                text: "";
                                placeholder-text: "搜索功能或执行命令...";
                                x: 12px;
                                y: 8px;
                                width: parent.width - 24px;
                                height: 24px;
                            }
                        }

                        Rectangle {
                            width: 42px;
                            height: 42px;
                            border-radius: 8px;
                            background: root.card-alt-bg;
                            border-width: 1px;
                            border-color: root.line-color;

                            Text {
                                text: "调";
                                color: root.text-color;
                                font-size: 13px;
                                font-weight: 600;
                                horizontal-alignment: center;
                                vertical-alignment: center;
                            }

                            TouchArea {
                                clicked => {
                                    root.theme-popup-open = !root.theme-popup-open;
                                }
                            }
                        }
                    }
                }

                HorizontalLayout {
                    spacing: 16px;
                    padding: 16px;

                    ScrollView {
                        width: 950px;

                        VerticalLayout {
                            spacing: 16px;

                            Text { text: "运营总览"; color: root.text-color; font-size: 20px; font-weight: 600; }
                            Text { text: "实时监控直播间状态、规则运行情况和互动数据"; color: root.muted-color; font-size: 12px; }

                            HorizontalLayout {
                                spacing: 12px;
                                StatusCard { width: 220px; height: 92px; label: "登录状态"; value: "已登录"; icon: "登"; accent: #34c759; base-bg: root.panel-bg; outline-color: root.line-color; text-color: root.text-color; muted-color: root.muted-color; }
                                StatusCard { width: 220px; height: 92px; label: "房间状态"; value: "直播中"; icon: "房"; accent: #34c759; base-bg: root.panel-bg; outline-color: root.line-color; text-color: root.text-color; muted-color: root.muted-color; }
                                StatusCard { width: 220px; height: 92px; label: "监听状态"; value: root.run-status; icon: "听"; accent: root.theme-primary; base-bg: root.panel-bg; outline-color: root.line-color; text-color: root.text-color; muted-color: root.muted-color; }
                                StatusCard { width: 220px; height: 92px; label: "WebSocket"; value: "已连接"; icon: "WS"; accent: root.theme-primary; base-bg: root.panel-bg; outline-color: root.line-color; text-color: root.text-color; muted-color: root.muted-color; }
                            }

                            HorizontalLayout {
                                spacing: 12px;
                                StatusCard { width: 220px; height: 92px; label: "AI 状态"; value: "就绪"; icon: "AI"; accent: #af52de; base-bg: root.panel-bg; outline-color: root.line-color; text-color: root.text-color; muted-color: root.muted-color; }
                                StatusCard { width: 220px; height: 92px; label: "本场弹幕"; value: root.session-danmu-count; icon: "弹"; accent: #5ac8fa; base-bg: root.panel-bg; outline-color: root.line-color; text-color: root.text-color; muted-color: root.muted-color; }
                                StatusCard { width: 220px; height: 92px; label: "礼物价值"; value: root.session-gift-value; icon: "礼"; accent: #ffcc00; base-bg: root.panel-bg; outline-color: root.line-color; text-color: root.text-color; muted-color: root.muted-color; }
                                StatusCard { width: 220px; height: 92px; label: "更新状态"; value: root.update-link == "" ? "已是最新" : "有新版本"; icon: "更"; accent: #ff2d55; base-bg: root.panel-bg; outline-color: root.line-color; text-color: root.text-color; muted-color: root.muted-color; }
                            }

                            HorizontalLayout {
                                spacing: 14px;
                                StatusCard { width: 220px; height: 92px; label: "进场 / 关注"; value: root.session-entry-count + " / " + root.session-follow-count; icon: "进"; accent: #5ac8fa; base-bg: root.panel-bg; outline-color: root.line-color; text-color: root.text-color; muted-color: root.muted-color; }
                                StatusCard { width: 220px; height: 92px; label: "大航海人数"; value: root.session-guard-buyer-count; icon: "舰"; accent: #af52de; base-bg: root.panel-bg; outline-color: root.line-color; text-color: root.text-color; muted-color: root.muted-color; }
                                StatusCard { width: 220px; height: 92px; label: "人气峰值 / 均值"; value: root.session-popularity; icon: "人"; accent: #ff3b30; base-bg: root.panel-bg; outline-color: root.line-color; text-color: root.text-color; muted-color: root.muted-color; }
                                StatusCard { width: 220px; height: 92px; label: "PK 概览"; value: root.session-pk-summary; icon: "PK"; accent: #ff9500; base-bg: root.panel-bg; outline-color: root.line-color; text-color: root.text-color; muted-color: root.muted-color; }
                            }

                            Rectangle {
                                background: root.panel-bg;
                                border-radius: 8px;
                                border-width: 1px;
                                border-color: root.line-color;

                                VerticalLayout {
                                    padding: 18px;
                                    spacing: 12px;

                                    Text { text: "用户详情查询"; color: root.text-color; font-size: 16px; font-weight: 600; }

                                    HorizontalLayout {
                                        spacing: 10px;
                                        LineEdit { text <=> root.user-query-uid; placeholder-text: "输入 UID"; }
                                        ActionButton {
                                            width: 120px;
                                            label: "查询用户"; primary: true; accent: root.theme-primary; fill: root.card-alt-bg; text-color: root.text-color; line-color: root.line-color;
                                            clicked => { root.query-user-detail(root.user-query-uid); }
                                        }
                                    }

                                    Text {
                                        text: root.user-detail-text;
                                        color: root.text-color;
                                        font-size: 12px;
                                        wrap: word-wrap;
                                    }
                                }
                            }

                            Rectangle {
                                background: root.panel-bg;
                                border-radius: 8px;
                                border-width: 1px;
                                border-color: root.line-color;

                                VerticalLayout {
                                    padding: 18px;
                                    spacing: 14px;

                                    Text { text: "快速操作"; color: root.text-color; font-size: 16px; font-weight: 600; }

                                    HorizontalLayout {
                                        spacing: 10px;
                                        ActionButton { label: "生成登录链接"; primary: true; accent: root.theme-primary; fill: root.card-alt-bg; text-color: root.text-color; line-color: root.line-color; clicked => { root.start-login(); } }
                                        ActionButton { label: "检查登录"; accent: root.theme-primary; fill: root.card-alt-bg; text-color: root.text-color; line-color: root.line-color; clicked => { root.check-login(); } }
                                        ActionButton { label: "检测房间"; accent: root.theme-primary; fill: root.card-alt-bg; text-color: root.text-color; line-color: root.line-color; clicked => { root.check-room(); } }
                                        ActionButton { label: "启动监听"; primary: true; accent: root.theme-primary; fill: root.card-alt-bg; text-color: root.text-color; line-color: root.line-color; clicked => { root.start-monitor(); } }
                                    }

                                    HorizontalLayout {
                                        spacing: 10px;
                                        ActionButton { label: "停止监听"; accent: root.theme-primary; fill: root.card-alt-bg; text-color: root.text-color; line-color: root.line-color; clicked => { root.stop-monitor(); } }
                                        ActionButton { label: "读取配置"; accent: root.theme-primary; fill: root.card-alt-bg; text-color: root.text-color; line-color: root.line-color; clicked => { root.load-config(); } }
                                        ActionButton {
                                            label: "保存配置"; primary: true; accent: root.theme-primary; fill: root.card-alt-bg; text-color: root.text-color; line-color: root.line-color;
                                            clicked => {
                                                root.save-config(
                                                    root.room-id, root.danmu-len, root.entry-msg, root.goodbye-info,
                                                    root.pk-notice, root.welcome-enabled, root.entry-effect, root.thanks-gift,
                                                    root.thanks-focus, root.thanks-share, root.cron-danmu, root.keyword-reply,
                                                    root.draw-by-lot, root.sign-in-enable,
                                                    root.danmu-filter-enable, root.danmu-filter-words-text,
                                                    root.danmu-filter-repeat-threshold, root.gift-aliases-text,
                                                    root.gift-thanks-templates-text, root.gift-summary-thanks,
                                                    root.gift-summary-template, root.newcomer-danmu-enable,
                                                    root.newcomer-danmu-template, root.permanent-blacklist-users-text,
                                                    root.permanent-blacklist-names-text, root.special-nicknames-text,
                                                    root.robot-name, root.talk-robot-cmd,
                                                    root.robot-mode-index, root.chatgpt-token, root.chatgpt-api-url, root.chatgpt-prompt,
                                                    root.welcome-list-text, root.focus-list-text, root.blacklist-wide-text,
                                                    root.blacklist-exact-text, root.keyword-reply-text, root.cron-danmu-text,
                                                    root.draw-list-text
                                                );
                                            }
                                        }
                                        ActionButton { label: "检查更新"; accent: root.theme-primary; fill: root.card-alt-bg; text-color: root.text-color; line-color: root.line-color; clicked => { root.check-update(); } }
                                    }

                                    HorizontalLayout {
                                        spacing: 10px;
                                        Rectangle {
                                            height: 42px;
                                            border-radius: 8px;
                                            background: root.card-alt-bg;
                                            border-width: 1px;
                                            border-color: root.line-color;

                                            LineEdit {
                                                text <=> root.manual-danmu;
                                                placeholder-text: "输入手动弹幕内容...";
                                                x: 12px;
                                                y: 8px;
                                                width: parent.width - 24px;
                                                height: 24px;
                                            }
                                        }
                                        ActionButton { label: "发送弹幕"; primary: true; accent: root.theme-primary; fill: root.card-alt-bg; text-color: root.text-color; line-color: root.line-color; clicked => { root.send-danmu(root.manual-danmu); } }
                                        ActionButton {
                                            label: "下载更新器";
                                            enabled: root.update-link != "";
                                            accent: root.theme-primary; fill: root.card-alt-bg; text-color: root.text-color; line-color: root.line-color;
                                            clicked => { root.download-update(root.update-link); }
                                        }
                                    }
                                }
                            }

                            Rectangle {
                                background: root.panel-bg;
                                border-radius: 8px;
                                border-width: 1px;
                                border-color: root.line-color;

                                VerticalLayout {
                                    padding: 18px;
                                    spacing: 12px;

                                    Text { text: "自动化功能"; color: root.text-color; font-size: 16px; font-weight: 600; }

                                    HorizontalLayout {
                                        spacing: 12px;
                                        FeatureCard { width: 440px; title: "弹幕欢迎"; desc: "普通用户进入直播间"; meta: "12 条规则"; enabled: root.welcome-enabled; accent: root.theme-primary; base-bg: root.card-alt-bg; outline-color: root.line-color; text-color: root.text-color; muted-color: root.muted-color; }
                                        FeatureCard { width: 440px; title: "舰长欢迎"; desc: "舰长/提督/总督进入"; meta: "3 种模板"; enabled: root.entry-effect; accent: root.theme-primary; base-bg: root.card-alt-bg; outline-color: root.line-color; text-color: root.text-color; muted-color: root.muted-color; }
                                    }
                                    HorizontalLayout {
                                        spacing: 12px;
                                        FeatureCard { width: 440px; title: "礼物答谢"; desc: "收到礼物自动感谢"; meta: "15s 聚合"; enabled: root.thanks-gift; accent: root.theme-primary; base-bg: root.card-alt-bg; outline-color: root.line-color; text-color: root.text-color; muted-color: root.muted-color; }
                                        FeatureCard { width: 440px; title: "AI 机器人"; desc: "智能弹幕对话"; meta: root.robot-mode-index == 1 ? "ChatGPT 模式" : "QingYunKe 模式"; enabled: true; accent: #af52de; base-bg: root.card-alt-bg; outline-color: root.line-color; text-color: root.text-color; muted-color: root.muted-color; }
                                    }
                                    HorizontalLayout {
                                        spacing: 12px;
                                        FeatureCard { width: 440px; title: "秒级定时弹幕"; desc: "按 Cron 精确发送"; meta: root.cron-danmu ? "2 个任务运行中" : "当前关闭"; enabled: root.cron-danmu; accent: #5ac8fa; base-bg: root.card-alt-bg; outline-color: root.line-color; text-color: root.text-color; muted-color: root.muted-color; }
                                        FeatureCard { width: 440px; title: "抽签与签到"; desc: "互动命令自动响应"; meta: "抽签 / 签到 / 查询弹幕"; enabled: root.draw-by-lot || root.sign-in-enable; accent: #ffcc00; base-bg: root.card-alt-bg; outline-color: root.line-color; text-color: root.text-color; muted-color: root.muted-color; }
                                    }
                                }
                            }

                            HorizontalLayout {
                                spacing: 16px;

                                Rectangle {
                                    width: 452px;
                                    background: root.panel-bg;
                                    border-radius: 8px;
                                    border-width: 1px;
                                    border-color: root.line-color;

                                    VerticalLayout {
                                        padding: 18px;
                                        spacing: 10px;
                                        Text { text: "今日弹幕趋势"; color: root.text-color; font-size: 16px; font-weight: 600; }
                                        Text { text: "10:00 - 15:00"; color: root.muted-color; font-size: 11px; }
                                        HorizontalLayout {
                                            spacing: 10px;
                                            Rectangle { width: 40px; height: 96px; border-radius: 8px; background: root.line-color; }
                                            Rectangle { width: 40px; height: 120px; border-radius: 8px; background: root.theme-primary; y: 18px; }
                                            Rectangle { width: 40px; height: 142px; border-radius: 8px; background: root.theme-primary; y: 0px; }
                                            Rectangle { width: 40px; height: 112px; border-radius: 8px; background: root.line-color; y: 28px; }
                                            Rectangle { width: 40px; height: 150px; border-radius: 8px; background: root.theme-primary; y: 0px; }
                                            Rectangle { width: 40px; height: 170px; border-radius: 8px; background: root.theme-primary; y: 0px; }
                                        }
                                    }
                                }

                                Rectangle {
                                    width: 452px;
                                    background: root.panel-bg;
                                    border-radius: 8px;
                                    border-width: 1px;
                                    border-color: root.line-color;

                                    VerticalLayout {
                                        padding: 18px;
                                        spacing: 12px;
                                        Text { text: "互动分布"; color: root.text-color; font-size: 16px; font-weight: 600; }
                                        Rectangle { height: 26px; border-radius: 13px; background: root.line-color; }
                                        Rectangle { height: 26px; width: 360px; border-radius: 13px; background: root.theme-primary; }
                                        Rectangle { height: 26px; width: 180px; border-radius: 13px; background: #ff2d55; }
                                        Rectangle { height: 26px; width: 120px; border-radius: 13px; background: #34c759; }
                                        Rectangle { height: 26px; width: 74px; border-radius: 13px; background: #ffcc00; }
                                        Text { text: "弹幕 1247 · 礼物 89 · 关注 34 · 分享 12"; color: root.muted-color; font-size: 11px; }
                                    }
                                }
                            }

                            Rectangle {
                                background: root.panel-bg;
                                border-radius: 8px;
                                border-width: 1px;
                                border-color: root.line-color;

                                VerticalLayout {
                                    padding: 18px;
                                    spacing: 14px;

                                    Text { text: "规则编辑工作区"; color: root.text-color; font-size: 16px; font-weight: 600; }

                                    HorizontalLayout {
                                        spacing: 10px;
                                        Rectangle { height: 32px; width: 96px; border-radius: 8px; background: root.theme-primary; Text { text: "基础配置"; color: #ffffff; font-size: 11px; font-weight: 600; horizontal-alignment: center; vertical-alignment: center; } }
                                        Rectangle { height: 32px; width: 84px; border-radius: 8px; background: root.card-alt-bg; border-width: 1px; border-color: root.line-color; Text { text: "AI 配置"; color: root.text-color; font-size: 11px; font-weight: 600; horizontal-alignment: center; vertical-alignment: center; } }
                                        Rectangle { height: 32px; width: 96px; border-radius: 8px; background: root.card-alt-bg; border-width: 1px; border-color: root.line-color; Text { text: "列表规则"; color: root.text-color; font-size: 11px; font-weight: 600; horizontal-alignment: center; vertical-alignment: center; } }
                                        Rectangle { height: 32px; width: 96px; border-radius: 8px; background: root.card-alt-bg; border-width: 1px; border-color: root.line-color; Text { text: "运行策略"; color: root.text-color; font-size: 11px; font-weight: 600; horizontal-alignment: center; vertical-alignment: center; } }
                                    }

                                    HorizontalLayout {
                                        spacing: 14px;
                                        VerticalLayout {
                                            spacing: 8px;
                                            Text { text: "直播间 ID"; color: root.muted-color; font-size: 11px; font-weight: 600; }
                                            LineEdit { text <=> root.room-id; placeholder-text: "直播间 ID"; }
                                            Text { text: "进房弹幕"; color: root.muted-color; font-size: 11px; font-weight: 600; }
                                            LineEdit { text <=> root.entry-msg; }
                                            Text { text: "下播公告"; color: root.muted-color; font-size: 11px; font-weight: 600; }
                                            LineEdit { text <=> root.goodbye-info; }
                                            Text { text: "弹幕长度"; color: root.muted-color; font-size: 11px; font-weight: 600; }
                                            SpinBox { value <=> root.danmu-len; minimum: 1; maximum: 100; }
                                        }

                                        VerticalLayout {
                                            spacing: 8px;
                                            Text { text: "自动化开关"; color: root.text-color; font-size: 13px; font-weight: 600; }
                                            CheckBox { text: "PK 提醒"; checked <=> root.pk-notice; }
                                            CheckBox { text: "欢迎"; checked <=> root.welcome-enabled; }
                                            CheckBox { text: "特效欢迎"; checked <=> root.entry-effect; }
                                            CheckBox { text: "礼物答谢"; checked <=> root.thanks-gift; }
                                            CheckBox { text: "关注答谢"; checked <=> root.thanks-focus; }
                                            CheckBox { text: "分享答谢"; checked <=> root.thanks-share; }
                                            CheckBox { text: "定时弹幕"; checked <=> root.cron-danmu; }
                                            CheckBox { text: "关键词回复"; checked <=> root.keyword-reply; }
                                            CheckBox { text: "弹幕过滤"; checked <=> root.danmu-filter-enable; }
                                            CheckBox { text: "新人发言提示"; checked <=> root.newcomer-danmu-enable; }
                                            CheckBox { text: "礼物总结答谢"; checked <=> root.gift-summary-thanks; }
                                        }

                                        VerticalLayout {
                                            spacing: 8px;
                                            Text { text: "AI 机器人"; color: root.text-color; font-size: 13px; font-weight: 600; }
                                            LineEdit { text <=> root.robot-name; placeholder-text: "机器人名称"; }
                                            LineEdit { text <=> root.talk-robot-cmd; placeholder-text: "触发词"; }
                                            ComboBox { model: ["QingYunKe", "ChatGPT"]; current-index <=> root.robot-mode-index; }
                                            LineEdit { text <=> root.chatgpt-api-url; placeholder-text: "ChatGPT API URL"; }
                                            LineEdit { text <=> root.chatgpt-token; placeholder-text: "ChatGPT API Token"; }
                                            LineEdit { text <=> root.chatgpt-prompt; placeholder-text: "Prompt"; }
                                        }
                                    }
                                }
                            }

                            Rectangle {
                                background: root.panel-bg;
                                border-radius: 8px;
                                border-width: 1px;
                                border-color: root.line-color;

                                VerticalLayout {
                                    padding: 18px;
                                    spacing: 12px;

                                    Text { text: "列表规则配置"; color: root.text-color; font-size: 16px; font-weight: 600; }
                                    Text { text: "沿用当前功能字段，但改成更像桌面工作台的排版"; color: root.muted-color; font-size: 11px; }

                                    Text { text: "欢迎语（每行一条，支持 {user}）"; color: root.muted-color; font-size: 11px; font-weight: 600; }
                                    TextEdit { text <=> root.welcome-list-text; height: 82px; wrap: word-wrap; }
                                    Text { text: "关注 / 分享附加答谢"; color: root.muted-color; font-size: 11px; font-weight: 600; }
                                    TextEdit { text <=> root.focus-list-text; height: 64px; wrap: word-wrap; }
                                    Text { text: "黑名单与关键词回复"; color: root.muted-color; font-size: 11px; font-weight: 600; }

                                    HorizontalLayout {
                                        spacing: 10px;
                                        TextEdit { text <=> root.blacklist-wide-text; height: 74px; wrap: word-wrap; }
                                        TextEdit { text <=> root.blacklist-exact-text; height: 74px; wrap: word-wrap; }
                                    }

                                    TextEdit { text <=> root.keyword-reply-text; height: 84px; wrap: word-wrap; }
                                    Text { text: "弹幕过滤词与重复阈值"; color: root.muted-color; font-size: 11px; font-weight: 600; }
                                    HorizontalLayout {
                                        spacing: 10px;
                                        TextEdit { text <=> root.danmu-filter-words-text; height: 74px; wrap: word-wrap; }
                                        SpinBox { value <=> root.danmu-filter-repeat-threshold; minimum: 2; maximum: 20; }
                                    }
                                    Text { text: "礼物别名与指定答谢模板（每行 key=value）"; color: root.muted-color; font-size: 11px; font-weight: 600; }
                                    HorizontalLayout {
                                        spacing: 10px;
                                        TextEdit { text <=> root.gift-aliases-text; height: 74px; wrap: word-wrap; }
                                        TextEdit { text <=> root.gift-thanks-templates-text; height: 74px; wrap: word-wrap; }
                                    }
                                    Text { text: "新人提示、礼物总结与观众规则"; color: root.muted-color; font-size: 11px; font-weight: 600; }
                                    HorizontalLayout {
                                        spacing: 10px;
                                        LineEdit { text <=> root.newcomer-danmu-template; placeholder-text: "新人提示模板"; }
                                        LineEdit { text <=> root.gift-summary-template; placeholder-text: "礼物总结模板"; }
                                    }
                                    HorizontalLayout {
                                        spacing: 10px;
                                        TextEdit { text <=> root.permanent-blacklist-users-text; height: 74px; wrap: word-wrap; }
                                        TextEdit { text <=> root.permanent-blacklist-names-text; height: 74px; wrap: word-wrap; }
                                        TextEdit { text <=> root.special-nicknames-text; height: 74px; wrap: word-wrap; }
                                    }
                                    TextEdit { text <=> root.cron-danmu-text; height: 84px; wrap: word-wrap; }
                                    TextEdit { text <=> root.draw-list-text; height: 84px; wrap: word-wrap; }
                                }
                            }

                            Rectangle {
                                background: root.panel-bg;
                                border-radius: 8px;
                                border-width: 1px;
                                border-color: root.line-color;

                                VerticalLayout {
                                    padding: 18px;
                                    spacing: 12px;

                                    Text { text: "功能规划"; color: root.text-color; font-size: 16px; font-weight: 600; }

                                    HorizontalLayout {
                                        spacing: 12px;
                                        PlanCard { width: 288px; title: "多房间监测"; desc: "同时监控多个直播间状态、弹幕和 PK 活动。"; tags: "多账号 · 批量管理"; accent: root.theme-primary; base-bg: root.card-alt-bg; outline-color: root.line-color; text-color: root.text-color; muted-color: root.muted-color; }
                                        PlanCard { width: 288px; title: "用户详情查询"; desc: "拉取观众最近弹幕、送礼和粉丝牌画像。"; tags: "UID 查询 · 粉丝分析"; accent: root.theme-primary; base-bg: root.card-alt-bg; outline-color: root.line-color; text-color: root.text-color; muted-color: root.muted-color; }
                                        PlanCard { width: 288px; title: "更强 PK 分析"; desc: "展示对手房间、助攻数据和 PK 历史。"; tags: "胜率预测 · 对手分析"; accent: root.theme-primary; base-bg: root.card-alt-bg; outline-color: root.line-color; text-color: root.text-color; muted-color: root.muted-color; }
                                    }

                                    HorizontalLayout {
                                        spacing: 12px;
                                        PlanCard { width: 288px; title: "弹幕过滤器"; desc: "识别刷屏、敏感词、疑似机器人和风险账号。"; tags: "内容审核 · 条件禁言"; accent: root.theme-primary; base-bg: root.card-alt-bg; outline-color: root.line-color; text-color: root.text-color; muted-color: root.muted-color; }
                                        PlanCard { width: 288px; title: "点歌系统"; desc: "弹幕点歌、排队、黑名单和多音源切换。"; tags: "互动工具 · 音乐播放"; accent: root.theme-primary; base-bg: root.card-alt-bg; outline-color: root.line-color; text-color: root.text-color; muted-color: root.muted-color; }
                                        PlanCard { width: 288px; title: "录播与素材"; desc: "自动录播、截图、OBS 网页源和素材管理。"; tags: "视频存档 · 网页源"; accent: root.theme-primary; base-bg: root.card-alt-bg; outline-color: root.line-color; text-color: root.text-color; muted-color: root.muted-color; }
                                    }
                                }
                            }
                        }
                    }

                    Rectangle {
                        width: 304px;
                        background: root.panel-bg;
                        border-radius: 8px;
                        border-width: 1px;
                        border-color: root.line-color;

                        VerticalLayout {
                            padding: 16px;
                            spacing: 12px;

                            Rectangle {
                                background: root.card-alt-bg;
                                border-radius: 8px;
                                border-width: 1px;
                                border-color: root.line-color;

                                VerticalLayout {
                                    padding: 14px;
                                    spacing: 8px;
                                    Text { text: "账号与连接"; color: root.text-color; font-size: 14px; font-weight: 600; }
                                    Text { text: "当前账号  " + root.user-info; color: root.text-color; font-size: 12px; }
                                    Text { text: "Token 状态  有效"; color: #34c759; font-size: 12px; }
                                    Text { text: "房间 ID  " + root.room-id; color: root.text-color; font-size: 12px; }
                                    Text { text: "心跳状态  正常"; color: root.muted-color; font-size: 12px; }
                                }
                            }

                            Rectangle {
                                background: root.card-alt-bg;
                                border-radius: 8px;
                                border-width: 1px;
                                border-color: root.line-color;

                                VerticalLayout {
                                    padding: 14px;
                                    spacing: 8px;
                                    Text { text: "运行状态"; color: root.text-color; font-size: 14px; font-weight: 600; }
                                    Text { text: "当前监听  活跃"; color: root.text-color; font-size: 12px; }
                                    Text { text: "发送队列  3 条待发"; color: root.text-color; font-size: 12px; }
                                    Text { text: "定时任务  2 个运行"; color: root.text-color; font-size: 12px; }
                                    Text { text: "AI 请求  就绪"; color: #34c759; font-size: 12px; }
                                    Text { text: "礼物聚合  15s 延迟"; color: root.muted-color; font-size: 12px; }
                                }
                            }

                            Rectangle {
                                background: root.panel-strong-bg;
                                border-radius: 8px;
                                border-width: 1px;
                                border-color: root.line-color;

                                VerticalLayout {
                                    padding: 14px;
                                    spacing: 10px;
                                    Text { text: "实时日志"; color: #f8fafc; font-size: 15px; font-weight: 600; }
                                    ScrollView {
                                        height: 560px;
                                        Text {
                                            text: root.log-text == "" ? "暂无运行日志" : root.log-text;
                                            color: root.muted-color;
                                            font-size: 12px;
                                            wrap: word-wrap;
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }

        if root.theme-popup-open : Rectangle {
            x: 1160px;
            y: 78px;
            width: 332px;
            height: 530px;
            border-radius: 8px;
            background: root.popup-bg;
            border-width: 1px;
            border-color: root.line-color;

            VerticalLayout {
                padding: 18px;
                spacing: 14px;

                HorizontalLayout {
                    Text { text: "主题设置"; color: root.text-color; font-size: 16px; font-weight: 600; }
                    Rectangle { }
                    Rectangle {
                        width: 28px;
                        height: 28px;
                        border-radius: 8px;
                        background: root.card-alt-bg;
                        border-width: 1px;
                        border-color: root.line-color;

                        Text {
                            text: "X";
                            color: root.text-color;
                            font-size: 11px;
                            font-weight: 600;
                            horizontal-alignment: center;
                            vertical-alignment: center;
                        }

                        TouchArea {
                            clicked => {
                                root.theme-popup-open = false;
                            }
                        }
                    }
                }

                Text { text: "外观模式"; color: root.text-color; font-size: 13px; font-weight: 600; }
                HorizontalLayout {
                    spacing: 10px;
                    Rectangle {
                        height: 42px;
                        border-radius: 8px;
                        background: !root.dark-mode ? root.theme-primary : root.card-alt-bg;
                        border-width: 1px;
                        border-color: !root.dark-mode ? root.theme-primary : root.line-color;

                        Text {
                            text: "浅色";
                            color: !root.dark-mode ? #ffffff : root.text-color;
                            font-size: 12px;
                            font-weight: 600;
                            horizontal-alignment: center;
                            vertical-alignment: center;
                        }

                        TouchArea { clicked => { root.dark-mode = false; } }
                    }
                    Rectangle {
                        height: 42px;
                        border-radius: 8px;
                        background: root.dark-mode ? root.theme-primary : root.card-alt-bg;
                        border-width: 1px;
                        border-color: root.dark-mode ? root.theme-primary : root.line-color;

                        Text {
                            text: "深色";
                            color: root.dark-mode ? #ffffff : root.text-color;
                            font-size: 12px;
                            font-weight: 600;
                            horizontal-alignment: center;
                            vertical-alignment: center;
                        }

                        TouchArea { clicked => { root.dark-mode = true; } }
                    }
                }

                Text { text: "6 种主题颜色"; color: root.text-color; font-size: 13px; font-weight: 600; }
                HorizontalLayout {
                    spacing: 8px;
                    ThemeSwatch { width: 98px; swatch: #007aff; label: "系统蓝"; selected: root.theme-choice == 0; fill: root.card-alt-bg; text-color: root.text-color; line-color: root.line-color; activate => { root.theme-choice = 0; } }
                    ThemeSwatch { width: 98px; swatch: #34c759; label: "翡翠绿"; selected: root.theme-choice == 1; fill: root.card-alt-bg; text-color: root.text-color; line-color: root.line-color; activate => { root.theme-choice = 1; } }
                    ThemeSwatch { width: 98px; swatch: #ff9500; label: "日落橙"; selected: root.theme-choice == 2; fill: root.card-alt-bg; text-color: root.text-color; line-color: root.line-color; activate => { root.theme-choice = 2; } }
                }
                HorizontalLayout {
                    spacing: 8px;
                    ThemeSwatch { width: 98px; swatch: #ff2d55; label: "兰花粉"; selected: root.theme-choice == 3; fill: root.card-alt-bg; text-color: root.text-color; line-color: root.line-color; activate => { root.theme-choice = 3; } }
                    ThemeSwatch { width: 98px; swatch: #8e8e93; label: "系统灰"; selected: root.theme-choice == 4; fill: root.card-alt-bg; text-color: root.text-color; line-color: root.line-color; activate => { root.theme-choice = 4; } }
                    ThemeSwatch { width: 98px; swatch: #ffcc00; label: "琥珀金"; selected: root.theme-choice == 5; fill: root.card-alt-bg; text-color: root.text-color; line-color: root.line-color; activate => { root.theme-choice = 5; } }
                }

                Text { text: "自定义色板"; color: root.text-color; font-size: 13px; font-weight: 600; }
                HorizontalLayout {
                    spacing: 8px;
                    ThemeSwatch { width: 68px; swatch: #af52de; label: "紫"; selected: root.theme-choice == 6; fill: root.card-alt-bg; text-color: root.text-color; line-color: root.line-color; activate => { root.theme-choice = 6; } }
                    ThemeSwatch { width: 68px; swatch: #ff3b30; label: "红"; selected: root.theme-choice == 7; fill: root.card-alt-bg; text-color: root.text-color; line-color: root.line-color; activate => { root.theme-choice = 7; } }
                    ThemeSwatch { width: 68px; swatch: #5ac8fa; label: "青"; selected: root.theme-choice == 8; fill: root.card-alt-bg; text-color: root.text-color; line-color: root.line-color; activate => { root.theme-choice = 8; } }
                    ThemeSwatch { width: 68px; swatch: #af52de; label: "紫"; selected: root.theme-choice == 9; fill: root.card-alt-bg; text-color: root.text-color; line-color: root.line-color; activate => { root.theme-choice = 9; } }
                }
                HorizontalLayout {
                    spacing: 8px;
                    ThemeSwatch { width: 68px; swatch: #22c55e; label: "草绿"; selected: root.theme-choice == 10; fill: root.card-alt-bg; text-color: root.text-color; line-color: root.line-color; activate => { root.theme-choice = 10; } }
                    ThemeSwatch { width: 68px; swatch: #5856d6; label: "靛蓝"; selected: root.theme-choice == 11; fill: root.card-alt-bg; text-color: root.text-color; line-color: root.line-color; activate => { root.theme-choice = 11; } }
                    Rectangle { }
                    Rectangle { }
                }

                Rectangle {
                    border-radius: 8px;
                    background: root.card-alt-bg;
                    border-width: 1px;
                    border-color: root.line-color;

                    VerticalLayout {
                        padding: 14px;
                        spacing: 10px;
                        Text { text: "预览效果"; color: root.text-color; font-size: 12px; font-weight: 600; }
                        Rectangle {
                            height: 36px;
                            border-radius: 8px;
                            background: root.theme-primary;
                            Text {
                                text: "主要按钮";
                                color: #ffffff;
                                font-size: 12px;
                                font-weight: 600;
                                horizontal-alignment: center;
                                vertical-alignment: center;
                            }
                        }
                        HorizontalLayout {
                            spacing: 10px;
                            Rectangle {
                                width: 40px;
                                height: 22px;
                                border-radius: 11px;
                                background: root.theme-primary;
                                Rectangle { x: 20px; y: 3px; width: 16px; height: 16px; border-radius: 8px; background: #ffffff; }
                            }
                            Rectangle {
                                width: 74px;
                                height: 22px;
                                border-radius: 11px;
                                background: root.theme-primary;
                                Text {
                                    text: "状态标签";
                                    color: #ffffff;
                                    font-size: 10px;
                                    font-weight: 600;
                                    horizontal-alignment: center;
                                    vertical-alignment: center;
                                }
                            }
                        }
                    }
                }

                ActionButton { label: "恢复默认"; accent: root.theme-primary; fill: root.card-alt-bg; text-color: root.text-color; line-color: root.line-color; clicked => { root.theme-choice = 0; root.dark-mode = false; } }
            }
        }
    }
}
